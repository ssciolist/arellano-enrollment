<h1>All Students</h1>

<ul>
  <% @students.each do |student| %>
    <li><%= student.name %></li>
    <%= link_to 'Delete', student_path(student),id: "student_id_#{student.id}", method: 'delete' %>
  <% end %>
</ul>
